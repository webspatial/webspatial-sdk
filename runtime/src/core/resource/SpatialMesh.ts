import { SpatialObject } from '../SpatialObject'

/**
 * Mesh asset containing geometry
 * [TODO] should the naming of resources end in Resource like component?
 */
export class SpatialMesh extends SpatialObject {}
