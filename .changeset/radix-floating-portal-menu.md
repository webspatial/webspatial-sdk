---
'@webspatial/react-sdk': minor
---

Add `useSpatialPortalContainer()` so Radix and similar libraries can portal floating UI into the nearest SpatialDiv window document body. `Spatialized2DElementContainer` now provides `SpatialWindowContext` for portal content.

---