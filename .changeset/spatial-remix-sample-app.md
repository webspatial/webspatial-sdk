---
"@webspatial/react-sdk": patch
---

Add `apps/spatial-remix-min`, a consumer-shaped **React Router 7** (Remix-style) **Vite + SSR** fixture for `@webspatial/react-sdk`: lazy entry + loader-based `User-Agent`, `jsxImportSource`, `react-router-dom`, and dev middleware to swallow Chrome DevTools `/.well-known` probes.

Document the app in README / migration guide / product alignment / AGENTS.
