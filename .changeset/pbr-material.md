---
'@webspatial/core-sdk': minor
'@webspatial/react-sdk': minor
'@webspatial/platform-visionos': minor
---

Add physically-based materials: `SpatialSession.createPBRMaterial()`, the `<PBRMaterial>` React component (also via `<Material type="pbr">`) with `MaterialPresets`, and RealityKit-backed rendering on visionOS.
