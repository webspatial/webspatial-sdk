---
'@webspatial/react-sdk': patch
---

Keep SpatialDiv portal stylesheets when a newer parent-head sync starts during load, and retry failed stylesheet loads so enable-xr content keeps its layout styles.
