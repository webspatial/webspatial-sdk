{
  "name": "@webspatial/shared",
  "version": "0.1.4",
  "description": "",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "start": "npm run dev",
    "build": "tsup",
    "dev": "tsup --watch",
    "format": "npx prettier --write 'src/**/*.ts' 'src/**/*.tsx' || true",
    "test": "tsc && npm run coverage",
    "coverage": "vitest run --coverage",
    "test:w": "vitest --watch"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.14",
    "tsup": "^8.4.0"
  }
}
