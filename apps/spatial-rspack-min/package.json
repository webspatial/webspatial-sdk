{
  "name": "spatial-rspack-min",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Minimal Rspack + React fixture for WebSpatial SDK lazy entry.",
  "scripts": {
    "dev": "rspack serve --mode development",
    "build": "tsc && rspack build --mode production",
    "test": "tsc && rspack build --mode production"
  },
  "dependencies": {
    "@webspatial/react-sdk": "workspace:*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@rspack/cli": "^1.7.12",
    "@rspack/core": "^1.7.12",
    "@types/node": "^24.13.3",
    "@types/react": "^18.3.31",
    "@types/react-dom": "^18.3.7",
    "typescript": "^5.9.3"
  }
}
