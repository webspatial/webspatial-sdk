{
  "buildCommand": "node esbuild.mjs --build",
  "outputDirectory": "dist",
  "ignoreCommand": "[[ \"$VERCEL_GIT_COMMIT_REF\" == \"main\" ]] && exit 1 || exit 0",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
