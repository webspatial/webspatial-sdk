import { CapabilityDemo } from '@/components/CapabilityDemo'

export const metadata = { title: '/capability-wrapper — spatial-next-min' }

export default function CapabilityWrapperPage() {
  return <CapabilityDemo />
}
